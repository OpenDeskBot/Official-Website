# 小歪 OpenDeskBot 官网

## 项目简介

开源桌面机器人 **小歪 OpenDeskBot** 的产品展示页。页面含 3D 机器人预览、滚动关键帧调参、TTS 口型同步演示（「点我说话」）。

- GitHub 组织：[OpenDeskBot](https://github.com/OpenDeskBot)
- 主入口：`index.html`（与 `index(1).html` 内容同步）

## 目录结构

| 路径 | 说明 |
|------|------|
| `index.html` | 官网主页 |
| `assets/xiaowai-intro.wav` | 开场白音频（随仓库发布，GitHub Pages 直接访问） |
| `scripts/embed_audio.py` | 从 TTS 导出目录复制 wav 到 `assets/` |
| `.nojekyll` | GitHub Pages 禁用 Jekyll，避免静态资源被忽略 |
| `tts_export_1779355029_9f61c778/` | TTS 口型时间轴、视素 SVG、原始 wav |
| `*.svg` | 表情资源 |

## 音频（GitHub Pages 最简单方案）

开场白为静态文件 `assets/xiaowai-intro.wav`，页面内：

```js
new Audio('assets/xiaowai-intro.wav')
```

**无需本地服务器、无需 Base64**。推送到 GitHub 后 Settings → Pages 选分支即可访问。

更新 TTS 源文件后：`python scripts/embed_audio.py`

## GitHub Pages 最少文件

- `index.html`
- `assets/xiaowai-intro.wav`
- `.nojekyll`

Three.js 仍从 CDN 加载，需联网。口型时间轴已写在 `index.html` 内。

## 最近更新（2026-05-21）

- 品牌改为 **小歪 OpenDeskBot**，文案由「乐高/积木/拼插」调整为「灵动/拟人」
- GitHub 链接指向 `https://github.com/OpenDeskBot`
- 开场白音频改为 `assets/xiaowai-intro.wav` 静态文件（适配 GitHub Pages）
- 亮点区：AI 电子宠物 / 画画 / 插嘴标注「未来计划」并置底
- 玩法区顺序：AI 桌面助手 → 注视感知 → 控制米家（计划支持）
- 「成本」改为「硬件清单」；上手路径改为三步（免账号、免费 ASR/TTS、可自部署）
